import {StyleSheet} from "react-native";

const styles= StyleSheet.create({
    textStyle:{
        fontSize: 10,
     
    },
    loadMoreButtom:{
        padding: 20,
        textAlign: 'center',
        backgroundColor: '#eee',
        borderRadius: 3,
    },
});

export default styles;